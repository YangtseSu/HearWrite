# 更新日志（面向用户，中文）

> 给用户看的版本说明。每次发版在打 tag 前追加一节；GitHub Release 正文
> 由 workflow 自动取对应小节。`## [x.y.z] — 日期` 标题必须与 tag
> `vx.y.z` 一一对应（无 `v` 前缀）。

## [0.8.0] — 2026-09-16

### 词库

- **仁爱版初中 七年级上、下册与八年级上册**入库，共 72 份词表 / 1545 词：每个 Unit 按课本栏目拆成「导入 / 探究 / 拓展 / 小结」四张表，同一词多词性用行内标注（`fine | adj. | 身体好的，健康的；很好；v. 对……处以罚款；n. 罚款`），朗读与显示与其他词表一致
- 内置词库合计 **651 份词表 / 21769 行**

### 听写

- **长词不再只剩省略号**：词语字号随长度自动缩放，仍放不下时盘下出现「展开全部」，可看完整词、词性与释义；盘内内容保证落在圆内（大字号同样成立），不再出现「看得见半句、找不到展开入口」
- **「标记错词」可撤销**：再点一次即「取消标记」。错词本按原样回滚——此前已有的错次、时间与来源原样保留，本场新建的行删除，不会把学生前几次的错误一起抹掉
- 成绩线恒显示（`正确 N 词 · 错词 0 · 满分`）；顶栏改显示**本场**错词数，与全局错词本不再混为一谈
- 暂停时盘内提示改为「已暂停」，不再与播放中同形
- 本场出现发音失败时明确提示（本场有 N 次发音失败，成绩可能不准），不再与「一切正常」同形
- 结束页的错词 chips 最多 24 个，超出给「查看全部 N 个错词」；间隔 ± 按钮命中区加大到 48dp
- 成绩卡「再听一遍」与「复习错词」主次分开并各自标注范围；「拍照批改」未配置识词服务时当场提示，不再等拍完才报

### 统计

- 趋势图 14 根柱子恒可见：没听写的日子画灰桩，有词的日至少 6dp（此前 0 词的柱子高度为 0，图上等于不存在，「哪天没听写」看不出来）
- 图表左上角标「最高 N 词」，横轴改为周几 + 起止日期；点按柱子读出当天「9月14日 周一 · 2 场 · 45 词」
- 最近记录默认显示 20 场，可「查看全部 N 场记录」；记录再多也不会一次全部加载
- 高频错词行补上来源词表名，来源仍存在时整行可点跳到该词表
- 听写记录读取失败会明确提示并可「重试」（此前显示「暂无听写记录」，像是在说从没听写过）
- 连续天数带上单位，今天有记录时显示「今日已打卡」

### 首页与词库

- 草稿读取中显示「正在读取草稿…」，「开始听写」同步忙碌（此前会先闪一帧「共 0 词」，点下去回答「请先输入单词列表」）
- 「清空」「示例」不再静默覆盖已有草稿，都会先确认；0 词时「清空」不可点
- 展示列表行点击只用于选定起始词，释义展开改为独立按钮；新增「重置为从第 1 词开始」
- 从历史记录 / 收藏载入后抽屉自动关闭，提示落在屏幕上（此前提示被抽屉自己的遮罩挡住）
- 进入编辑态立刻聚焦并弹出键盘；空词表从零输入无缝衔接
- 多选统一叫「多选词表」，跨表重复词合并有明确提示
- 抽词池里读不出的词表会被具名列出，不再只有日志知道

### 设置

- 新增**应用内开源许可页**（关于 → 开源许可，GPL-3.0 全文）
- 服务商表单字段回车跳到下一项
- 「清除配置」明确破坏性样式；清除正在生效的发音配置时说明「发音来源将退回有道词典」
- 无缓存时「清空发音缓存」不可点
- 子页往返后设置页保持原来的滚动位置

### 其他

- 历史记录与错词本的单行删除支持「撤销」，按原行恢复（含错次、时间、来源与收藏标记）
- 修复草稿编辑态下底部播放面板预留空间为 0，导致「英文示例 / 汉字示例」掉到「开始听写」下方的问题
- 日期显示统一为 `9月14日` / `9月14日 20:10`（非当年补年份）；系统语音为空时给出说明，而不是连同「英文使用默认音色」开关一起消失
- 依赖更新（Compose BOM 2026.09.00、Room 2.8.5、Navigation 2.10.1）；修正 CI 上因 Google 下线 `tools` 包而失败的构建步骤

## [0.7.0] — 2026-09-14

### 词库

- **人教版小学语文 一年级上、下册**入库：《义务教育教科书·语文》一上的识字表 / 写字表 / 汉语拼音（63 表）与一下的识字表 / 写字表（72 表）——这两册教材只印字、不印拼音与组词，应用会用内置数据自动补全
- **人教版初中语文 七年级上、下册 读读写写**入库，共 38 课 491 词；八上原有的 18 份词表补上了课文标题（如「八上 读读写写 15」→「八上 15 背影 读读写写」），三个年级的命名现在一致
- 内置词库合计 579 份词表 / 20224 行

### 修复

- 修复词库预览的「随机顺序」开关读屏时没有名称、且只有开关本身可点（「随机顺序」几个字点不动）的问题——现在与首页同样，整行都是开关、可读可点
- 修复一处词表名称排序的隐患：七年级语文词表标题里的汉字此前未进入排序表，会退化成按字形排序，进而排错位置

### 其他

- 修复若干界面细节：设置页开关整行可点、拍照批改的「写：…」长答案不再被截断一行、批改页返回键回到成绩卡、清空错词本先确认、系统「完成」类提示改为应用内提示条（不再被系统弹窗样式覆盖）
- 主题字阶补齐 12 档（此前只定义了 5 档，多数中文文字用的是拉丁字阶的默认字距与行高）
- 汉字提示改用专属朱砂色，与红色（错误）、金色（收藏）区分开

## [0.6.0] — 2026-09-11

### 新功能

- **拍照批改**：听写结束后拍下学生写在纸上的答案，应用逐词比对本次词表并给出成绩（正确 / 错词 / 漏答 / 多余作答）；拼写接近、空白与多余的作答标记「存疑」提示人工核对，核对确认后才记入错词本，与手动标记的错词一样累计次数与来源。识别沿用拍照识词的同一服务（可先框选答案区域），语言按本场词表自动判断
- **多选词库 / 抽词听写**：在词库里跨分类勾选多份词表，底栏显示已选词表数与合计词数，可随机抽 X 个词听写（X 可填，默认全部即全量随机）；跨表重复词自动合并，抽词场次标错的词仍能回到原词表
- **再听一遍**：结束页新增按钮，按本场原顺序再听一遍，不必回首页重开
- **复习错词带原词行**：结束页与首页错词本的「复习错词 / 听写错词」会还原每个错词原本的词性与释义（英文）或拼音与组词（汉字），不再只剩光秃秃的单词
- **汉字提示自动补全**：粘贴或拍照得到的大字表里，单个汉字会自动补上拼音与组词（完全离线），组词朗读同样可用
- 新增内置词库分类 **义务教育语文课程标准**：《义务教育语文课程标准（2022 年版）》附录 4 基本字表 300 字、附录 5 常用字表表一 2500 字 + 表二 1000 字

### 其他

- 新增词库数据质检脚本与[词表贡献指南](docs/WORDLIST.md)；CI 在构建前校验全部内置词表与派生数据，保证词库改动不引入坏行
- 拍照识词与拍照批改共用同一套相机 / 相册取图与框选流程

## [0.5.0] — 2026-09-10

### 新功能

- 错词本记录每个错词的**错误次数与来源**（哪份词表 / 哪条历史 / 手动输入），跨次听写累积
- 错词本按来源分组、组内按错次从多到少排列，每行显示「错 N 次 · 最近 …」
- 内置词表来源可点「查看词表」跳回该词表预览；来源已被删除时显示「未知来源」，不删错词
- 同一场内重复标记只计一次；听写结束后的「复习错词」轮次不重复累计错次
- 新增**听写统计**页（首页「更多」→「听写统计」）：完整听完一场听写即自动记录场次、词数、错词率与用时；页面显示累计词数与错词率、听写天数与连续天数、正式听写 / 复习错词的拆分、最近 14 天词数趋势、高频错词与最近记录，可清空记录（错词本不受影响）
- 中途结束的听写不计入统计——只有听完的才算一场

### 其他

- 新增本地数据库升级的自动化测试（错词本、听写记录两类数据），保证老版本升级安装不丢数据

## [0.4.0] — 2026-09-09

### 新功能

- 系统语音新增音色选择：中文 / 英文可分别选用引擎音色并逐一试听，引擎默认音色也可试听；只有单一音色的引擎自动隐藏选择器
- 系统语音与微软 Edge 的英文音色合并为一个下拉列表，美式 / 英式直接同列挑选
- 词库列表行显示词数（如「103 词」）；内置英文词库（如 中考1600）预览时用离线词典自动补全词性 / 释义，点按可展开长释义
- 词库预览行与首页词表统一为词语在上、拼音 / 释义在下的排布
- 首页播放面板显示「从第 X 词开始」，与词库预览一致
- 词表编辑框自动填满整块区域（键盘弹出时收缩）；空词表直接以编辑外观开始，从零输入无缝衔接
- 编辑态按系统返回键等于「完成」，不再误退到后台
- 拍照识词弹层的「修改 / 去设置」直达识词服务商设置页
- 框选识别区域支持 TalkBack：可移动、缩放、重置选区，并实时播报选区范围

### 修复

- 修复间隔设为 1 秒时缺失最后一秒的提示音
- 修复取消自定义 TTS 试听导致共享下载去重失效（可能重复生成并重复计费）
- 修复再次听写收藏或历史词表时重复写入历史记录
- 清空或裁剪历史记录时保留已收藏的词表（提示文案同步更新）
- 修复全角竖线（如 月｜yuè｜月亮）行被整行朗读，拼音与组词混入语音
- 修复 OCR 接口地址缺少 http(s) 前缀时应用崩溃；预设服务商地址锁定不可误改，仅「自定义」可手填
- 修复设置文件损坏时启动崩溃，自动恢复默认设置
- 修复退出词表编辑时丢失最后约 500 毫秒的输入
- 修复键盘收起瞬间编辑卡片先顶满屏幕再弹回
- 修复播放按钮文字与无障碍播报不一致（播放 / 暂停 / 继续）
- 修复无 StrongBox 设备上 API Key 可能被明文保存：自动改用系统普通密钥加密并提示

## [0.3.2] — 2026-09-07

### 修复

- 修复系统语音在小米 / 澎湃（HyperOS）设备上无法播放：应用此前未声明 TTS 引擎可见性，即使已安装并启用 Google TTS 也会初始化失败；现在可正常使用，且引擎安装 / 启用后无需重启应用即可恢复
- 修复听写播放停止时一个跨线程可见性问题（停止可能不立即打断正在播放的发音）

### 安全

- TTS / 拍照识词服务商的 API Key 改用 Android Keystore 加密后存入本机（AES-256-GCM，密钥芯片内生成、不导出）：应用私有目录、备份文件与 root 读取下均为密文；之前已保存的 Key 不受影响，下次保存自动转为加密存储

### 其他

- 最低系统版本降至 Android 13（API 33），支持更多设备安装

## [0.3.1] — 2026-09-06

### 其他

- 发布包改规范命名 `HearWrite-0.3.1.apk`（含版本号，便于归档区分）；混淆映射随包提供 `HearWrite-0.3.1-mapping.txt`

## [0.3.0] — 2026-09-06

### 新功能

- 全新纸墨双主题：浅色纸面 / 深色墨夜，听写、首页、词库、设置同步换装
- 听写界面改版：大倒计时环、显示词语 / 标记错词并排按钮、间隔步进器 + 自动播放单卡片
- 首页直达词库与设置；拍照识词收进单词列表标题栏
- 发音来源改为单选列表：每个音源在行内展开自己的配置（试听、Edge 音色、服务商选择）
- 自定义 TTS 接口类型改为下拉选择（/audio/speech 与 Chat Completions）
- 拍照识词语言选择改为英文 / 中文页签
- 保存后的 API Key 只显示后四位提示，输入即替换

### 修复

- 修复自定义 TTS 表单地址与模型填不上的问题；无可用配置时明确提示暂用系统语音
- 切换音源、音色或服务商后，旧的试听结果不再残留
- 恢复听写底部的结束按钮（走二次确认退出）
- 倒计时出现 / 消失不再顶动界面；大字号下不再裁字
- 空格输入不再点亮测试 / 保存按钮

## [0.2.0] — 2026-09-05

### 新功能

- 微软 Edge 朗读：免费、无需 API Key 的神经网络语音，可分别选择中文 / 英文音色并试听
- 小米 MiMo 音色下拉选择 + 试听；英文可单独选用不同音色
- 有道词典、系统语音新增「测试并试听」
- 组词短语在 Edge / 自定义音源下用本音源朗读（有道词典音仍走系统语音）
- 启动器图标在中文系统下显示「听写」
- TTS / OCR 服务商预设整理（被精简的预设可走「自定义」继续使用）

### 修复

- 修复 Edge 语音音频帧解析错位导致的试听失败
- 无拼音单字（如「好」）组词不再被非常用读音劫持
- 英文音色选择（如 Sonia）在重启后保留
## [0.1.0] — 2026-09-04

首个签名发布。英文 / 汉字两种听写模式（生字自动组词朗读、多音字处理）；
10 套教材 403 份内置词库（可搜索、预览、载入草稿）；粘贴导入 + 拍照识词
（可框选识别区域，英文 / 汉字两种识别）；ECDICT 离线词典自动补全词性释义；
有道真人发音 / 系统语音 / 自定义 TTS 三档发音；错词本、历史 50 条、收藏；
语速 / 间隔 / 自动播报 / 朗读释义实时可调；深色主题。

